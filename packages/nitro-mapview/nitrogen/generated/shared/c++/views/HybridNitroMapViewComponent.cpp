///
/// HybridNitroMapViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "HybridNitroMapViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <NitroModules/PropNameIDCache.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::nitromapview::views {

  extern const char HybridNitroMapViewComponentName[] = "NitroMapView";

  HybridNitroMapViewProps::HybridNitroMapViewProps(const react::PropsParserContext& context,
                                                   const HybridNitroMapViewProps& sourceProps,
                                                   const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    region([&]() -> CachedProp<std::optional<Region>> {
      try {
        const react::RawValue* rawValue = rawProps.at("region", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.region;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<Region>>::fromRawValue(*runtime, value, sourceProps.region);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroMapView.region: ") + exc.what());
      }
    }()),
    onRegionChange([&]() -> CachedProp<std::optional<std::function<void(const Region& /* region */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("onRegionChange", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.onRegionChange;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const Region& /* region */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.onRegionChange);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroMapView.onRegionChange: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroMapViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridNitroMapViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, PropNameIDCache::get(*runtime, "f")), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("NitroMapView.hybridRef: ") + exc.what());
      }
    }()) { }

  bool HybridNitroMapViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("region"): return true;
      case hashString("onRegionChange"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridNitroMapViewComponentDescriptor::HybridNitroMapViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridNitroMapViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                        const std::shared_ptr<const react::Props>& props,
                                                                                        react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridNitroMapViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridNitroMapViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = static_cast<HybridNitroMapViewShadowNode&>(shadowNode);
    const std::shared_ptr<const HybridNitroMapViewProps>& constProps = concreteShadowNode.getConcreteSharedProps();
    const std::shared_ptr<HybridNitroMapViewProps>& props = std::const_pointer_cast<HybridNitroMapViewProps>(constProps);
    HybridNitroMapViewState state{props};
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::nitromapview::views
